[package]
name = "example-hello-convergence"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
converge-kernel = { path = "../../crates/kernel" }
converge-pack = { path = "../../crates/pack" }
async-trait.workspace = true
tokio.workspace = true
